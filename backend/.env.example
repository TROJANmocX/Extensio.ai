# Extensio.ai Backend Environment Configuration

# Port to run the Express API server (default is 5000)
PORT=5000

# Google Gemini API Key. Go to Google AI Studio to get a key: https://aistudio.google.com/
# If left blank, the server will operate in offline/mock generator fallback mode.
GEMINI_API_KEY=
